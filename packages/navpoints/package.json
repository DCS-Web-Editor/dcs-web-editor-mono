{
  "name": "@dcs-web-editor-mono/navpoints",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "scripts": {
    "dev": "vite",
    "clean": "rimraf dist/ && mkdir dist",
    "tsc": "tsc",
    "build": "npm run clean && tsc && vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vite-plugin-singlefile": "^0.13.2"
  },
  "devDependencies": {
    "rimraf": "^4.1.2"
  }
}
