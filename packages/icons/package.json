{
  "name": "icons",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist/ && mkdir dist",
    "tsc": "tsc",
    "build": "npm run copy-assets && tsc && vite build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy-assets": "npm run clean && cp -r ./src/small/ ./dist/small" 
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vite-plugin-singlefile": "^0.13.2"
  }
}
